# A counter using React and Redux

## Installation

Run the following command to install all the dependencies

`npm i`

## Start the app

To start the app, just run the command:

`npm start`

** app not finished yet **
